---
interface Props {
  text: string;
  highlight?: boolean;
}

const { text, highlight } = Astro.props;
---

{
  text && (
    <h3
      class={`font-medium text-md leading-8 text-balance ${highlight ? "text-yellow" : ""}`}
    >
      {text}
    </h3>
  )
}
