---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

{
  text && (
    <h3 class="text-xl font-semibold tracking-tight text-[--color-text-primary] md:text-2xl xl:text-3xl">
      {text}
    </h3>
  )
}
