---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

{
  text && (
    <div class="py-1 px-3 bg-dark-blue w-full border-t-1 border-r-1 border-l-1 border-black">
      <h2 class="font-medium text-lg leading-8 text-balance">{text}</h2>
    </div>
  )
}
